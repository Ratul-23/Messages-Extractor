[project]
name = "messages-extractor"
version = "0.1.0"
description = "Extracts *Messages*.xml files from Wizard101's Root.wad archive into a local, revision-versioned directory."
requires-python = ">=3.13"
dependencies = [
    "katsuba>=0.4.0",
]
